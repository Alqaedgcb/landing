<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LEXA AI - المتجر الذكي العصري</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Tajawal', sans-serif;
      background: linear-gradient(135deg, #030303 0%, #101820 100%);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      text-align: center;
      overflow-x: hidden;
      position: relative;
    }

    .hero {
      padding: 40px 20px;
      max-width: 700px;
      animation: fadeIn 1.5s ease-in-out forwards;
    }

    .hero h1 {
      font-size: 2.5rem;
      margin-bottom: 15px;
      background: linear-gradient(90deg, #00ffcc, #00ccff);
      -webkit-background-clip: text;
      color: transparent;
    }

    .hero p {
      font-size: 1.15rem;
      line-height: 1.9;
      color: #d1d1d1;
      margin-bottom: 25px;
    }

    .cta {
      display: inline-block;
      padding: 16px 38px;
      background: #25D366;
      border-radius: 50px;
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 1.2rem;
      box-shadow: 0 0 25px rgba(37,211,102,0.4);
      transition: all 0.3s ease;
    }

    .cta:hover {
      transform: scale(1.08);
      box-shadow: 0 0 35px rgba(37,211,102,0.8);
    }

    .tagline {
      font-size: 1rem;
      margin-top: 10px;
      color: #00ffcc;
      animation: pulse 2s infinite alternate;
    }

    footer {
      margin-top: 60px;
      padding: 20px;
      font-size: 0.9rem;
      background: rgba(255,255,255,0.05);
      width: 100%;
      text-align: center;
      color: #bbb;
    }

    footer span {
      display: inline-block;
      margin: 0 10px;
    }

    .bg-glow {
      position: absolute;
      width: 600px;
      height: 600px;
      background: radial-gradient(circle, rgba(0,255,200,0.2), transparent 70%);
      top: -150px;
      right: -100px;
      filter: blur(100px);
      animation: float 7s ease-in-out infinite alternate;
      z-index: 0;
    }

    @keyframes float {
      from { transform: translateY(0px); }
      to { transform: translateY(50px); }
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse {
      from { opacity: 0.7; }
      to { opacity: 1; }
    }

    /* ==== مكون المحادثة ==== */
    .chat-bubble {
      position: fixed;
      bottom: 90px;
      right: 20px;
      background: #fff;
      color: #000;
      border-radius: 15px;
      padding: 12px 16px;
      font-size: 0.95rem;
      max-width: 260px;
      box-shadow: 0 0 15px rgba(0,0,0,0.3);
      opacity: 0;
      transform: translateY(20px);
      transition: all 0.5s ease;
      line-height: 1.6;
      z-index: 1000;
    }

    .chat-bubble.show {
      opacity: 1;
      transform: translateY(0);
    }

    .chat-btn {
      position: fixed;
      bottom: 20px;
      right: 20px;
      width: 60px;
      height: 60px;
      background: #25D366;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 20px rgba(37,211,102,0.5);
      cursor: pointer;
      z-index: 999;
      transition: 0.3s ease;
    }

    .chat-btn:hover { transform: scale(1.1); }

    .chat-btn img {
      width: 35px;
      height: 35px;
    }
  </style>
</head>
<body>
  <div class="bg-glow"></div>

  <section class="hero">
    <h1>LEXA AI</h1>
    <p>
      متجر ذكي قائم على الذكاء الاصطناعي 🚀  
      يدمج التقنية، الفهم السلوكي، وتحليل السوق ليبيع منتجاته بنفسه.  
      مستقبل التجارة يبدأ من هنا.
    </p>

    <a href="https://wa.me/9677XXXXXXX?text=مرحباً%20👋%20أرغب%20في%20معرفة%20المزيد%20عن%20LEXA%20AI%20المتجر%20الذكي."
       target="_blank"
       class="cta">
       تواصل عبر واتساب 💬
    </a>

    <div class="tagline">عرض محدود — أول 100 اشتراك فقط</div>
  </section>

  <!-- فقاعة الدردشة -->
  <div class="chat-bubble" id="chatBubble">
    👋 مرحباً! أنا مساعد LEXA الذكي 🤖  
    هل ترغب أن أساعدك في اختيار المنتج الأنسب لك؟
    <br><br>
    <a href="https://wa.me/967776086053?text=مرحباً%20LEXA%20AI%20أرغب%20في%20استشارتك%20حول%20أفضل%20المنتجات."
       target="_blank"
       style="display:inline-block;background:#25D366;color:#fff;padding:6px 14px;border-radius:8px;text-decoration:none;margin-top:8px;font-weight:bold;font-size:0.9rem;">
       نعم، تابع عبر واتساب 💬
    </a>
  </div>

  <!-- زر الدردشة -->
  <div class="chat-btn" id="chatBtn">
    <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" alt="Chat" />
  </div>

  <footer>
    <span>✅ شحن سريع</span>
    <span>🔒 ضمان الأمان</span>
    <span>💳 الدفع عند الاستلام</span>
  </footer>

  <script>
    const chatBubble = document.getElementById("chatBubble");
    const chatBtn = document.getElementById("chatBtn");

    // ظهور فقاعة الدردشة بعد 5 ثوانٍ
    setTimeout(() => {
      chatBubble.classList.add("show");
    }, 5000);

    // النقر على الزر يعرض أو يخفي الفقاعة
    chatBtn.addEventListener("click", () => {
      chatBubble.classList.toggle("show");
    });
  </script>
</body>
</html>
